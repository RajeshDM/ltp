\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage{aaai2027}  % DO NOT CHANGE THIS
% The serif, sans-serif, and monospaced fonts are loaded automatically by
% aaai2027.sty (newtxtext, helvet, courier). DO NOT add \usepackage{times},
% \usepackage{helvet}, \usepackage{courier}, or any other font package.
\usepackage[hyphens]{url}  % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm}  % DO NOT CHANGE THIS
\usepackage{natbib}  % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing  % DO NOT CHANGE THIS
%
% These are recommended to typeset algorithms but not required. See the subsubsection on algorithms. Remove them if you don't have algorithms in your paper.
\usepackage{algorithm}
\usepackage{algorithmic}

%
% Recommended for better-looking tables
\usepackage{booktabs}
\usepackage{multirow}
\PassOptionsToPackage{table}{xcolor}
\usepackage{amsfonts}
\usepackage{amsmath}

\usepackage[table,svgnames]{xcolor} % Added svgnames

%
% Keep the \pdfinfo as shown here. There's no need
% for you to add the /Title and /Author tags.
\pdfinfo{
/TemplateVersion (2027.1)
}

\setcounter{secnumdepth}{0} %May be changed to 1 or 2 if section numbers are desired.

% Convenience macros. The ladder is cumulative: each system is the previous
% one plus exactly one component, and its suffix names the LAST component it
% contains. Full GADAR is the top rung and carries no suffix.
\newcommand{\gadar}{GADAR}                 % + chain layer (top rung; the method)
\newcommand{\gadarstruct}{\gadar{}-STRUCT} % symbol-free structural features
\newcommand{\gadarbind}{\gadar{}-BIND}     % + lifted domain layer + binding layer
\newcommand{\union}{UNION}                 % control: symbol identity, D in weights
% Placeholder for results that do not exist yet - renders visibly so no
% placeholder survives to submission unnoticed.
\newcommand{\todonum}[1]{\textbf{[#1]}}

\title{GADAR: Generalizing Across Domains via Action Ranking for Classical Planning}

% Anonymized for AAAI-27 review. Camera-ready author block below (commented).
\author{
    Anonymous Submission
}
\affiliations{
    Anonymized for review
}

\iffalse
% Camera-ready author block
\title{GADAR: Generalizing Across Domains via Action Ranking for Classical Planning}
\author {
    Rajesh Mangannavar
    % TODO: co-authors
}
\affiliations {
    Oregon State University\\
    Corvallis, OR 97330, USA\\
    mangannr@oregonstate.edu
}
\fi

\begin{document}

\maketitle

\begin{abstract}

Learned policies for classical planning act without search, but each policy is tied to the domain it was trained on: network modules per action schema, parameters per predicate, or vocabulary-sized encodings mean a trained policy cannot even be executed on new predicates and action schemas. We present \gadar{} (Generalizing Across Domains via Action Ranking), a policy for classical planning that takes the domain description as input --- $\pi(s, G, D)$ --- and constructs grounded actions in domains never seen in training, zero-shot and without search. The capability is built in three cumulative steps, each a contribution and each the ablation of the next: (i) \emph{symbol-free structural features} replace symbol identity with structural signatures, and a \emph{lifted domain layer} embeds $D$ itself into every state graph, identifying predicates and action schemas by their declared roles --- which schema uses them, as precondition or effect, at which argument slots --- rather than by name; (ii) a \emph{binding layer} ties each ground action's applicability to the lifted precondition occurrences it instantiates, expressing search-free action construction in the vocabulary of the domain description; and (iii) a \emph{chain layer} makes the domain's own action structure explicit --- which schema produces what another schema needs, which schema destroys it, how far each schema stands from achieving the goal, and which applicable grounding would achieve an open goal atom or delete a true atom. The last step is what turns executability into transfer. On a domain held out of training entirely, \gadar{} solves 68\% of the large test instances --- where an identically-executed untrained network solves 0\% and the same system with the chain layer removed reaches 4\% --- and 100\% of the training-scale instances at a plan quality of 0.97 relative to a satisficing planner. It also exceeds the ceiling of a hand-coded goal-achievement oracle, so what transfers is learned lifted structure rather than a terminal trigger. \gadar{} is invariant to symbol renaming by construction.

\end{abstract}

% Uncomment the following to link to your code, datasets, an extended version or similar.
% You must keep this block between (not within) the abstract and the main body of the paper.
%\begin{links}
%    \link{Code}{https://aaai.org/example/code}
%    \link{Extended version}{https://aaai.org/example/extended-version}
%\end{links}

\section{Introduction}

Classical planning tackles the problem of finding action sequences to achieve
goals in deterministic environments. While traditional planners can solve
small problems optimally using search and heuristics, they often struggle
with scalability on complex problems \citep{complexity-planning,
fast_downward}. This has motivated research into learning-based planning: a
model trained on solved problems captures regularities that carry over to new
ones. Two families of learned guidance have developed largely in parallel.
Learned \emph{heuristics} estimate cost-to-go and guide a search algorithm
\citep{strips_hgn, karia2021learning, goose_v2}; learned \emph{policies}
select actions directly, executing plans greedily without search
\citep{toyer2020asnets, expressive_v1, gabar}.

Learned policies have proven effective at generalizing \emph{within} a
domain. ASNets learn per-schema network modules that transfer across problem
sizes \citep{toyer2020asnets}; relational GNN policies learn value functions
whose greedy execution solves instances far larger than those seen in
training \citep{expressive_v1, policy_beyond_c2}; GABAR encodes states as
action-centric graphs and constructs grounded actions by ranking ---
selecting an action schema by scoring schema-node embeddings, then each
argument by scoring object-node embeddings --- generalizing to instances
significantly larger than its training problems \citep{gabar}. These policies
share a design choice: the domain's schemas and predicates are reflected in
the network's structure, whether as modules, parameters, or feature
dimensions. This is a strength for within-domain generalization --- the
network is shaped to its domain --- but it ties each trained policy to the
domain it was trained on: the policy cannot be executed on a domain whose
schemas and predicates have no slots in the network.

For learned heuristics, a different treatment of the domain has emerged.
STRIPS-HGN computes structural features that make no reference to symbol
identity \citep{strips_hgn}, and GOOSE's lifted learning graph (LLG) goes
further, encoding the full domain description --- schemas, preconditions,
effects, argument bindings --- as part of the heuristic's input
\citep{goose_v2}. In GOOSE's domain-independent setting, a single heuristic
is trained on one collection of domains and evaluated on held-out ones. This
line of work establishes that the \emph{input} side of learning for planning
can be made domain-agnostic. Heuristics, however, delegate action
construction to search, and the corresponding step for policies --- which
must construct grounded actions themselves --- remains open; \citet{goose_v2}
identify lifted policy learning as future work. It is a natural next step,
and a nontrivial one: the same authors prove that message-passing networks
over the lifted graph cannot compute even basic heuristics such as
$h^{\mathrm{add}}$, and their domain-independent configuration is empirically
their most difficult setting.

In this work we take that step. We learn a policy $\pi(s, G, D)$ that
receives the domain description $D$ as input alongside the state $s$ and goal
$G$, and constructs grounded actions in domains never seen in training ---
zero-shot, without search. Two established routes frame the contribution:
classical planners are domain-general but buy that generality with search at
plan time, while learned policies execute reactively but specialize to one
domain at training time. $\pi(s, G, D)$ occupies the remaining corner --- a
single learned artifact that is both domain-general and search-free --- and
the question we study is what such a policy can transfer, not whether it
outraces search. Our key idea is to change what the network is told
about symbols: not \emph{which symbol} something is, but \emph{what role the
symbol plays} in the lifted domain --- to bind every ground action's
applicability to that lifted structure, and to make the domain's own action
structure --- which schema produces what another schema needs, and how far
each schema stands from the goal --- an explicit part of the input, so that
the ranking decoder can score actions over symbols it has never seen.

We build this capability as a \emph{ladder} of three systems, each adding
exactly one representational commitment to the previous one, so that every
adjacent comparison isolates one component (the suffix names the last
component a system contains; full \gadar{} is the top rung). \union{} is the
control: GABAR's symbol-identity features widened to the union of the
training domains' vocabularies --- multi-task learning with the domain in the
weights. \gadarbind{} replaces symbol identity with structural features
computable in any domain, making the policy \emph{executable} on unseen
domains, and adds the domain description to every state graph --- a
lifted domain layer of predicate-symbol and occurrence nodes, plus the
\emph{grounded--lifted binding layer} that annotates each applicable ground
action with links to the lifted precondition occurrences it instantiates. The
binding layer connects what the two prior lines each capture only partially
--- lifted domain structure on one side, ground applicability on the other
--- a connection GOOSE's own analysis motivates: they prove that message
passing on the lifted graph alone cannot compute even the additive
relaxation heuristic, and they name lifted policy learning as an open
problem \citep{goose_v2}. Structure in the graph, however, is not yet
transfer: a network
can only exploit domain structure that message passing actually reaches, and
the relation ``schema $A$ produces what schema $B$ consumes'' is, in the
binding-layer graph, two hops through a shared predicate hub that loses the
pairing. Full \gadar{} therefore adds the \emph{chain layer}: edges between
the occurrence nodes themselves (\emph{enables}, \emph{threatens}), a
per-schema goal distance computed by backward search over those edges,
goal-relevance marks on objects, and grounded effect edges from an applicable
grounding to the goal atoms it would achieve and the true atoms it would
delete. Every one of these is computed from $D$ and the goal alone, is binary
or one-hot, and refers to no symbol name --- gripper's pick--drop and
miconic's board--depart are, to this layer, the same object. Empirically this
last rung is what converts executability into transfer. Downstream of the
representation, all three systems share GABAR's encoder, ranking decoder, and
search-free execution unchanged, so every comparison varies the
representation rather than the architecture. The ranking objective, one
design choice among several in the single-domain setting, does additional
work here: rankings are scale-free across domains, whereas value functions
presuppose a scale shared between domains --- a point independently
corroborated for learned heuristics and Q-value policies
\citep{chrestien_optimize, mueller_qvalues}.

Our experiments follow a leave-one-domain-out protocol over the eight GABAR
benchmark domains. On Visitall, a policy trained on
the other seven domains and never exposed to Visitall's predicates, schemas,
or objects solves 68\% of the large test instances zero-shot, against
0\% for an identically-executed untrained network and 4\% for
the same system without the chain layer (\gadarbind{}); on the
training-scale instances it solves 100\% against 16\% for \gadarbind{}. It does so at plan quality 0.97 relative to a satisficing planner
--- near-planner-length plans, not lucky wandering ---
above the 50\% ceiling of a hand-coded oracle
that always prefers an applicable action directly satisfying an open goal
atom, which rules out the reading that the transfer is a terminal trigger in
disguise. The union-vocabulary control makes the contrast concrete: the same
architecture with symbol-identity features solves the domains it trained on
--- 100\% on Miconic, 86\% on Gripper in-domain --- and transfers exactly
nothing, 0\% on both splits of held-out Visitall. By construction, \gadar{} is invariant to renaming of
predicates, schemas, and objects --- a robustness that symbol-based
approaches, including current large reasoning models, do not share
\citep{llm_baseline}
% TODO: verify exact renaming numbers (o1: 97.8% -> 37.3% on Mystery
% Blocksworld) against the camera-ready of llm_baseline
--- and the same invariance yields a precise transfer ceiling: structurally
isomorphic domains with different semantics necessarily receive identical
behavior, which marks where zero-shot ends and adaptation must begin.

\textbf{Contributions.}
\begin{itemize}
\item The first domain-agnostic \emph{policy} for classical planning:
  $\pi(s, G, D)$ constructs grounded actions zero-shot in unseen domains,
  without search.
\item A graph representation whose novel relations --- the grounded--lifted
  \emph{binding} edges, expressing why each action is applicable in the
  vocabulary of $D$, and the \emph{chain} edges, expressing which schema
  produces or destroys what another needs and how far each stands from the
  goal --- are computable in any domain and invariant to symbol renaming.
\item An evaluation designed as a ladder: each system is the previous plus
  one component, so the method and its ablation study are the same object,
  with matched untrained and multi-task controls under an identical
  execution harness.
\end{itemize}

\section{Related Work}



\textbf{Learning heuristics across domains.} Learning domain-independent
heuristics has a long history; we focus on the neural line. STRIPS-HGN trains
hypergraph networks on structural features of the delete relaxation and
evaluates on domains unseen in training \citep{strips_hgn}. GOOSE introduces
graph representations of planning tasks for both grounded and lifted inputs;
its lifted learning graph encodes the domain description itself --- schemas,
preconditions, effects, and argument bindings via index features --- and its
domain-independent setting trains a single heuristic across many domains and
evaluates on held-out ones \citep{goose_v1, goose_v2}. WL-GOOSE obtains
stronger per-domain heuristics with classical machine learning over
Weisfeiler--Lehman features, at the cost of per-domain feature dictionaries
\citep{wl_goose}. \citet{karia2021learning} learn generalized heuristics over
canonical abstractions without requiring the model at evaluation time; more
recently, LLM-evolved heuristic ensembles have been shown to transfer to a
held-out IPC learning track \citep{llm_evolved_heuristics_2026}. These
works establish domain-agnostic \emph{inputs}, and we adopt their lesson ---
structural features and lifted domain graphs. They differ from our setting in
their output: a heuristic delegates action construction to search, which
remains in the execution loop. \citet{goose_v2} additionally prove an
expressiveness limitation of message passing on their lifted graph and name
lifted policy learning as an open problem; our binding layer is designed
precisely to add the information that a policy needs and their graph
provably lacks (their Theorem 4.3).

\textbf{Learned policies for planning.} Generalized policy learning aims at
policies that transfer across instances of a domain
\citep{approx_Policy_iter, relational_rl}. ASNets construct a network from
the domain's action schemas and ground atoms, with weight sharing across
instantiations of the same schema \citep{toyer2020asnets}. St{\aa}hlberg,
Bonet, and Geffner learn value functions with GNNs whose greedy policies
generalize across instance sizes, characterize their expressive power in
terms of two-variable logics, and extend the approach with policy gradients
and unsupervised objectives \citep{expressive_v1, unsupervised_gnn,
policy_gradients, policy_beyond_c2}. Relational deep RL approaches
similarly learn policies over relational state representations
\citep{relational_drl, sr_drl, relational_mdp_learning, rddl_gnn,
generalized_planning_deep_rl}. GABAR, which this work extends, represents
ground actions explicitly in the state graph and constructs actions
autoregressively by ranking \citep{gabar}. The setting has remained
per-domain in the most recent work as well: self-improving search-and-learn
loops train a relational value heuristic per domain and generalize across
instance sizes within it \citep{learning_to_search_2026}, lookahead-based
general policies are learned over collections of instances of a single
domain \citep{lookahead_policies_2026}, and Q-value policy learning is
studied explicitly under a per-domain framing \citep{mueller_qvalues}. In
all of these, the domain's
vocabulary is reflected in the network --- as per-schema modules,
per-predicate parameters, or vocabulary-sized encodings --- so the trained
policy is bound to its training domain. \gadar{} removes this binding by
moving the domain description from the parameters of the network into its
input.

\textbf{Ranking in planning.} Learning to rank has been used to order
states and heuristic values for search: linear ranking functions for beam
search \citep{ranking_old_2009}, RankSVM-based heuristic synthesis
\citep{ranking_og}, ranking-optimized heuristics \citep{chrestien_optimize},
and learned pairwise rankings for greedy best-first search \citep{gbfs_v2}.
GABAR instead ranks \emph{actions} locally within a state, avoiding the need
for a globally consistent function \citep{gabar}. In the cross-domain
setting this choice becomes load-bearing rather than convenient: a value
function shared across domains presupposes a common scale of goal distances
that does not exist, whereas local action rankings are scale-free.
\citet{mueller_qvalues} provide independent convergent evidence from the
single-domain setting: vanilla Q-value regression fails precisely because it
does not discriminate between actions, and is repaired by a ranking-style
margin objective.

\textbf{Large language models and planning.} LLM-based planners have been
evaluated extensively; systematic studies find that performance of large
reasoning models degrades sharply when symbols are renamed
\citep{llm_baseline}, indicating reliance on symbol identity. \gadar{} is
invariant to symbol renaming by construction. LLM code-synthesis approaches
that write per-domain programs require solved instances and validator loops
at test time, addressing a different question from zero-shot execution.

\section{Background and Problem Setup}

A classical planning problem is a pair $P = \langle D, I \rangle$, where $D$
is a first-order domain and $I$ contains instance-specific information. The
domain $D$ consists of a set of predicate symbols $\mathcal{P}$ with
associated arities and a set of action schemas $\mathcal{A}$. Each schema $a
\in \mathcal{A}$ is defined by a tuple of parameters $\Delta(a)$ representing
variables that can be instantiated, preconditions $pre(a)$, add effects
$add(a)$, and delete effects $del(a)$. The instance information $I$ is a
tuple $\langle O, s_0, G \rangle$, where $O$ is a finite set of objects,
$s_0$ is the initial state represented as a set of ground atoms, and $G$ is
the goal condition, also a set of ground atoms. A ground action $a(o_1,
\dots, o_k)$ substitutes objects for a schema's parameters; it is applicable
in state $s$ if $pre(a) \subseteq s$ under the substitution, and yields
successor $s' = (s \setminus del(a)) \cup add(a)$. A plan is a sequence of
applicable ground actions transforming $s_0$ into a state satisfying $G$.

GABAR addresses the following problem: given a domain $D$ and solved training
instances of different sizes, learn a relational policy $\pi_D(s, G)$ that
solves larger test instances of the \emph{same} domain \citep{gabar}. This
paper addresses the cross-domain version. Given a set of training domains
$D_1, \dots, D_n$, each with solved instances of small problems, learn a
\emph{single} policy $\pi(s, G, D)$ --- taking the domain description itself
as input --- that solves instances of a test domain $D_{\mathit{test}}
\notin \{D_1, \dots, D_n\}$, zero-shot. Execution is search-free and follows
GABAR's protocol: the highest-ranked applicable action is executed, a
monitor optionally avoids revisiting states, and execution ends at the goal,
at a dead end, or at a length bound.

\begin{figure*}[t]
\centering
% Source: figures/fig1_representation.html --- updated 2026-07-28 with the
% chain layer (enables edge, grounded deletes-true edge, goal-relevant object
% rings, chain legend block). PNG rendered headlessly 2026-07-28 and committed
% (no manual screenshot needed; re-render: tools note in figures/).
% the SVG area only, save on Overleaf as Figures/gadar_graph_v1.png).
\includegraphics[width=0.98\textwidth]{Figures/gadar_graph_v1.png}
\caption{The \gadar{} representation for a Blocksworld task. The
\emph{ground instance layer} (bottom) is GABAR's action-centric graph:
objects, ground literals of the state and goal (G- prefix), and ground
action nodes with action--object edges carrying parameter slots. The
\emph{lifted domain layer} (top) is built from $D$ alone: action schemas,
their precondition and effect occurrences, and predicate symbols. The two
novel edge types connect the layers: \emph{binding edges} (green, dashed)
link an object's slot on an applicable ground action to the lifted
precondition occurrences that the object satisfies, and
\emph{instantiation edges} (orange, dotted) link each ground literal to its
predicate symbol. Filled nodes are ground; outlined nodes are lifted. The
elements are cumulative along the ladder of systems: \union{} uses the
ground layer with symbol-identity features; \gadarbind{} makes those
features structural and adds the lifted layer --- predicate symbols,
occurrence nodes, instantiation, role, and
binding edges. Full \gadar{} adds the chain layer on top; its edges
(\emph{enables} and \emph{threatens} between occurrence nodes, and grounded
achieves-goal / deletes-true edges) are omitted from this drawing for
legibility and described in the text. No feature encodes symbol identity, so
the representation is invariant to renaming by construction.}
\label{fig:representation}
\end{figure*}

\begin{figure*}[t]
\centering
% Source: figures/fig2_system.html --- panel (b) redrawn 2026-07-28 as the
% THREE-rung ladder (UNION / GADAR-BIND / GADAR+chain, chain rung includes
% type compilation). PNG rendered headlessly 2026-07-28 and committed.
\includegraphics[width=0.98\textwidth]{Figures/gadar_system_v1.png}
\caption{\gadar{} system overview. (a) The domain description $D$ and the
state and goal are converted into a graph. (b) The ladder of domain
conditioning: three systems, each the previous plus exactly one
representational commitment --- \union{} (symbol one-hots over the joint
vocabulary; the control), \gadarbind{} (symbol-free structural features,
the lifted domain layer, and the grounded--lifted binding
layer of Figure~\ref{fig:representation}), and \gadar{} (plus the chain
layer: enables/threatens edges between occurrence nodes, per-schema goal
distance, goal-relevance marks, and grounded effect edges). Every
adjacent comparison isolates one component. (c) The GNN encoder performs
$L$ rounds of edge, node, and global updates, unchanged from GABAR. (d) The
GRU decoder constructs a grounded action autoregressively --- schema first,
then each argument --- by scoring schema- and object-node embeddings; no
component is sized by the number of schemas or objects.}
\label{fig:system}
\end{figure*}

\section{GADAR}

Given a planning instance in PDDL format together with its domain
description $D$, \gadar{} converts the pair into a graph, processes the
graph with a GNN encoder, and constructs a grounded action with a GRU-based
decoder that first selects an action schema and then each of its arguments
(Figure~\ref{fig:system}). The encoder and decoder are those of GABAR,
unchanged; what changes is the graph they operate on. We present the
representation as it is evaluated: a ladder of systems, each adding one
commitment --- structural features in place of symbol identity, then the
lifted domain layer with its binding layer, then the chain layer --- so that
each addition is certified by comparison with its predecessor.

\subsection{A Domain-Agnostic Graph Representation}

\textbf{Ground instance layer.} The base of the representation is GABAR's
action-centric graph \citep{gabar}: nodes for objects, for ground literals
of the current state and the goal (goal literals marked by a flag), and for
action schemas, together with a global node; edges connect literals to their
argument objects and schemas to the objects of their applicable groundings,
with parameter positions on the edges (Figure~\ref{fig:representation},
bottom). This graph is what makes search-free action construction possible:
the decoder selects among schema nodes and object nodes that are present in
the input.

\textbf{The graph.} A task $(s, G, D)$ is encoded as
$\mathcal{G} = (V, E, X, R)$. The node set is
\begin{equation*}
V \;=\; O \,\cup\, L \,\cup\, A \,\cup\, \Sigma \,\cup\, Q \,\cup\, \{g\},
\end{equation*}
where $O$ are the objects, $L$ the ground literals of the state and goal,
$A$ the action schemas (one node per schema, as in GABAR), $\Sigma$ the
predicate symbols of $D$ --- including one synthetic unary static predicate
per declared type, so that typed and predicate-typed domains present the
same structure --- $Q$ the \emph{occurrence nodes} $q_{a,k}$, one per
literal occurrence $k$ in the preconditions and effects of schema $a$, and
$g$ a global node. The edge set is a union of typed relations,
\begin{equation*}
E \;=\; E_{\mathrm{lit}} \cup E_{\mathrm{act}} \cup E_{\mathrm{inst}} \cup
E_{\mathrm{occ}} \cup E_{\mathrm{bind}} \cup E_{\mathrm{chain}} \cup
E_{\mathrm{eff}},
\end{equation*}
\begin{itemize}
\item $E_{\mathrm{lit}} \subseteq L \times O$, $E_{\mathrm{act}} \subseteq
  A \times O$: literals to their arguments and schemas to the objects of
  their \emph{applicable} groundings --- GABAR's ground layer, with argument
  and slot positions as edge features;
\item $E_{\mathrm{inst}} \subseteq (L \cup O) \times \Sigma$:
  \emph{instantiation}, each literal to its predicate's symbol and each
  object to its declared type's symbol;
\item $E_{\mathrm{occ}} \subseteq Q \times (\Sigma \cup A)$: each occurrence
  to its predicate symbol and to its schema, carrying the role
  $r(q) \in \{\mathrm{pre}, \mathrm{add}, \mathrm{del}\}$ and the
  argument-to-slot pairs $(i, j)$ of that occurrence;
\item $E_{\mathrm{bind}} \subseteq O \times Q$ --- the \emph{binding layer}:
  $(o, q_{a,k})$ whenever some applicable grounding of $a$ places $o$ at a
  slot that occurrence $k$ constrains;
\item $E_{\mathrm{chain}} \subseteq Q \times Q$ --- the \emph{chain layer}:
  $\textit{enables} = \{(q, q') : r(q) = \mathrm{add},\, r(q') =
  \mathrm{pre},\, \mathrm{pred}(q) = \mathrm{pred}(q')\}$ and
  \textit{threatens} likewise with $r(q) = \mathrm{del}$;
\item $E_{\mathrm{eff}} \subseteq O \times L$ --- grounded effect edges: for
  each applicable grounding, an instantiated add effect equal to an
  \emph{unsatisfied} goal atom $\ell$ (\textit{achieves-goal}), or a delete
  effect equal to a \emph{true} atom $\ell$ (\textit{deletes-true}), links
  the grounding's objects to $\ell$.
\end{itemize}
Node features $X : V \to \{0,1\}^{d}$ concatenate indicator segments: the
node's kind; the arity (clamped at global caps $k_p$, $k_a$) and a
static-predicate flag; a goal flag on literals; on schema nodes the
goal-distance one-hot $\mathrm{gdist}(a) \in \{0, 1, 2, 3{+}\}$, the BFS
depth over \textit{enables} from the schemas that add a goal predicate; and
on objects a goal-relevance bit (the object occurs in an open goal atom).
Edge features $R : E \to \{0,1\}^{k}$ concatenate the relation type with
position, slot, and role indicators up to the caps. The widths $d$ and $k$
are functions of $(k_p, k_a)$ alone --- no dimension is $|A|$, $|P|$,
$|T|$, or $|O|$ --- so a single network serves every domain, instances from
different domains batch together, and permuting symbol names leaves
$(X, R)$ unchanged: renaming invariance holds by construction. Every
feature is a flag or a one-hot; nothing is a magnitude. The goal distance
in particular is bucketed rather than numeric, deliberately: a number must
be read on a scale, and a scale shared across domains is precisely what the
cross-domain setting does not provide --- the same argument that makes
local action ranking, and not value regression, the right objective here.

\textbf{Cost.} $\Sigma$, $Q$, $E_{\mathrm{occ}}$, and $E_{\mathrm{chain}}$
are domain-sized constants computed once per domain; $\mathrm{gdist}$
depends only on which predicates occur in the goal and is computed once per
(domain, goal signature). Only $E_{\mathrm{bind}}$, $E_{\mathrm{eff}}$, and
the goal marks are per-state, each linear in the applicable groundings and
open goal atoms.

\textbf{Why these relations.} The lifted part of the graph
($\Sigma$, $Q$, $E_{\mathrm{inst}}$, $E_{\mathrm{occ}}$) is LLG-adjacent
and we credit its lineage: GOOSE first showed the full lifted domain can be
an input for learning \citep{goose_v2}; ours differs in engineering
(positions as edge features rather than index-node chains, undirected
message flow, a retained global node). The role differs more.
$E_{\mathrm{bind}}$ connects what the two prior lines capture only
separately: LLG has lifted preconditions but no ground actions, and GABAR
has ground applicability but indexed by per-domain strings that do not
transfer. Binding expresses \emph{why} each action is applicable in $D$'s
own vocabulary --- exactly the ground-level information whose absence from
the lifted graph GOOSE prove costly (their Theorem 4.3, no
$h^{\mathrm{add}}$). In Figure~\ref{fig:representation}, the edge placing
O3 in Unstack's first slot binds to the occurrences \emph{on(x,y)} and
\emph{clear(x)}, and survives unchanged if every symbol is renamed.
$E_{\mathrm{chain}}$ states the one relation planning turns on --- schema
$A$ produces what schema $B$ needs --- which in the binding graph exists
only as a two-hop path through a shared predicate hub whose aggregation
destroys the producer--consumer pairing. Because its endpoints are
occurrences, the pairing survives message passing, delete effects acquire a
consequence, and the invariant transfers across domains: gripper's
$\textsf{pick} \rightarrow \textsf{drop}$ and miconic's
$\textsf{board} \rightarrow \textsf{depart}$ are the \emph{same} object, an
\textit{enables} edge between an add- and a pre-occurrence of a shared
predicate. $E_{\mathrm{eff}}$ grounds the layer: the decoder scores object
nodes, and these edges put ``choosing this object closes an open goal'' and
``choosing it undoes something true'' one hop from the quantity being
ranked.



\textbf{Relation to GOOSE's LLG.} LLG and our lifted layer agree on the
essential idea --- the domain description as input --- and differ in encoding
(argument positions as edge features rather than chains of index nodes;
occurrence nodes carrying exact argument-to-slot pairs). The fundamental
difference is what the graph is \emph{for}. A heuristic graph must support
one number, read from a global embedding; LLG therefore contains no ground
actions and no applicability information, and \citet{goose_v2} prove that
message passing on it cannot even compute $h^{\mathrm{add}}$. A policy
graph must contain the decoder's targets --- schema nodes and object nodes
to score --- and the evidence for scoring them: which groundings are
applicable and why. GABAR showed the first half (ground actions in the
graph) with per-domain annotations that do not transfer; the binding layer
supplies the second half in $D$'s own vocabulary.

\subsection{A Ladder of Domain Conditioning}

The three systems we evaluate stand in a strict subset relation, each the
previous plus exactly one representational commitment:

\begin{itemize}
\item \union{} (control): GABAR's symbol-identity one-hots, widened to the
  union of the training domains' vocabularies. The domain stays in the
  weights; this is multi-task learning, not domain conditioning.
\item \gadarbind{}: symbol-free structural features replace the one-hots,
  making the policy executable in any domain, and the lifted domain layer
  --- predicate-symbol nodes, occurrence nodes, instantiation and role edges
  --- puts $D$ into every state graph, together with the binding edges that
  tie each applicable grounding's slots to the precondition occurrences it
  satisfies. Ground applicability is expressed in the vocabulary of $D$.
\item \gadar{}: plus the chain layer --- enables and threatens edges between
  occurrence nodes, per-schema goal distance, goal-relevant object marks, and
  grounded achieves-goal / deletes-true edges. The domain's own action
  structure, and its relation to the goal, become explicit.
\end{itemize}

\noindent Because adjacent systems differ in exactly one component, the
experimental comparison and the ablation study are the same object: the
\union{}-to-\gadarbind{} step measures what it costs to remove symbol
identity and what it buys to put the domain description in the graph
instead --- the component that connects ground applicability to the lifted
description --- and the
\gadarbind{}-to-\gadar{} step isolates the chain layer, which makes that
structure reachable by message passing. All three share one encoder input
convention: feature widths are fixed constants across domains (capped
maximum predicate and schema arities), so instances from different domains
mix freely within a training batch. The lifted layer adds a per-state cost
of domain-sized constants --- one symbol node per predicate, occurrence
nodes per schema literal --- plus one instantiation edge per ground
literal. The chain layer adds no node and no per-state computation beyond
the goal-relevance marks and the effect edges of applicable groundings: its
chaining edges and goal distances are functions of $(D, \text{goal
signature})$ and are computed once and cached. We report measured per-state
inference times alongside coverage.

\subsection{Action Construction and Training}

The decoder is GABAR's and we change it only where per-domain constants
would otherwise appear (Figure~\ref{fig:system}d). From the encoded graph,
a GRU selects an action schema by scoring the schema-node embeddings against
its hidden state, then selects each argument in turn by scoring object-node
embeddings, feeding each selection back as the next input. The argument loop
runs to the \emph{selected schema's} arity, read from the domain
description, under a global maximum-arity cap --- not a per-domain constant.
No component of the decoder is sized by the number of schemas, objects, or
predicates; selection is always a dot product between the decoder state and
the embedding of a node present in the input graph. This output-lifted
decoder is what allows the same network to construct actions in a domain
whose schemas it has never seen.

Training is supervised, as in GABAR: expert plans are collected with a
classical planner on small instances of each training domain, and the policy
is trained with cross-entropy to reproduce the expert's schema choice and
each argument choice at every state along the plans. Because feature widths
are domain-independent, instances from different domains mix freely within a
batch. The objective ranks actions \emph{locally within a state}, which is
what permits a shared network across domains: local rankings are scale-free,
while a value target would presuppose a scale of goal distances shared
between domains \citep{chrestien_optimize, mueller_qvalues}.

\section{Experimental Setup}

\textbf{Domains.} We evaluate on the eight benchmark domains used by GABAR
--- Blocks, Miconic, Spanner, Gripper, Visitall, Grid, Logistics, and
Rovers \citep{gabar, pddl-generators}. Training data consists of plans
computed by a classical planner \citep{fast_downward} on the small instances
of each training domain (between \todonum{125} and \todonum{312} instances
per domain); states along each plan are converted to graphs and the policy
is trained to reproduce the expert action as described above. As held-out
targets we report Visitall and Miconic, each on two splits, so that the
zero-shot numbers of every system are matched on the same splits.

\textbf{Protocol.} Two experiment families, run for every rung of the
ladder. (i)~\emph{Leave-one-domain-out} (the headline): for each target
domain, a single policy is trained on the remaining seven and evaluated on
the held-out domain zero-shot, having never seen its predicates, schemas, or
objects. (ii)~\emph{In-domain}: one policy trained on all eight domains and
evaluated on each, measuring what a shared domain-conditioned policy costs
relative to per-domain training (the price of generality).
Leave-one-domain-out is stricter than the single
fixed train/held-out split used in prior domain-independent evaluation
\citep{goose_v2}. Held-out domains are
evaluated on two splits: the \emph{hard} split is the domain's test set,
with instances up to an order of magnitude larger than training-scale
problems; the \emph{easy} split consists of the held-out domain's own
training-scale instances --- unseen by the policy, since the domain itself
was held out --- separating ``cannot handle the unseen domain'' from
``cannot scale within it.'' Training domains are evaluated on the hard
split only.

\textbf{Baselines and references.} Our primary comparison is the \union{}
control described above: identical data, architecture, training procedure,
and budget --- only the representation differs. Any zero-shot capability
\gadar{} has beyond this control is attributable to moving the domain into
the input. Two references bracket the zero-shot numbers. The \emph{floor} is
an untrained control held to strict protocol parity: the identical system
--- same graph, same decoder, same execution harness, same step bound ---
with a network that has seen a single epoch of training. It therefore
measures exactly what the harness achieves when the learned component
contributes nothing, under the same architecture. Because the executor is
monitored and bounded, easy splits are forgiving of aimless but legal
behavior; we therefore read easy-split coverage together with plan quality,
which aimless behavior cannot fake. The \emph{ceiling} is published
per-domain GABAR \citep{gabar}, trained directly on each held-out domain; it
sees the test domain in training and by construction cannot be executed
zero-shot, so it calibrates --- rather than competes with --- transfer, and
we read zero-shot coverage as the fraction of this ceiling that transfer
recovers. Finally, the satisficing planner that generates our training data
\citep{fast_downward} solves \todonum{$\approx$100}\% of the test instances
--- by search. As in all prior work on generalized policies, the planner is
the plan-quality reference, not a competitor; the object of study is what
a reactive, search-free policy can transfer.

\textbf{Ablations.} The ladder is the ablation study
(Figure~\ref{fig:system}b): \gadarbind{} is exactly \gadar{} with the chain
layer removed, and \union{} is the system with neither the lifted domain
layer nor symbol-free features --- the domain in the weights. No separate
ablation systems are
needed; every component's contribution is read off adjacent columns of
Table~\ref{table:main_table}, and all systems are evaluated on the same
held-out splits. We additionally report one diagnostic that is not a rung of
the ladder: an \emph{achievement oracle} that, at every state, prefers any
applicable ground action whose add effects directly satisfy an unsatisfied
goal atom, and otherwise defers to the policy. It upper-bounds what a
hand-coded terminal trigger can buy on each domain and thereby separates
``the network learned to fire when the goal is one step away'' from ``the
network learned to navigate toward the goal.''

\textbf{Metrics and implementation.} Following GABAR, we report coverage
(C), the percentage of test problems solved within the execution bound, and
plan quality (P), the ratio of the reference planner's plan length to the
policy's on jointly solved problems, so higher is better and values above 1
indicate plans shorter than the reference. Because P conditions on the
problems a system solves, systems with higher coverage are scored on harder
instances; coverage is therefore the primary metric and P is read within,
not across, columns. Encoder and decoder
hyperparameters are carried over from GABAR unchanged ($L{=}9$
message-passing rounds, 64-dimensional representations, GRU decoder); all
systems train for 500 epochs with batch size 64 and learning rate
$5{\times}10^{-4}$. Checkpoints are selected by combined training and
validation loss, and every saved checkpoint of every run is evaluated; the
test set plays no role in model selection. The reason for the second half of
that protocol is empirical and we report it in the results: zero-shot
transfer is not monotone in training progress.
% TODO: seeds, hardware, wall-clock training time, and measured per-state
% inference time for GADAR vs GADAR-BIND vs GADAR-STRUCT.
Renaming invariance is verified as a unit test: permuting all symbol names
in a domain yields identical input tensors.

\section{Results and Discussion}

% Remaining structure to fill as runs land: in-domain table (price of
% generality), diversity curve (Figure 3, C5), per-domain failure-mode
% discussion for the other three held-out targets.

\textbf{Zero-shot transfer to an unseen domain.} The headline result is the
Visitall block of Table~\ref{table:main_table}. A single policy trained on the
other seven benchmark domains --- never exposed to Visitall's predicates,
schemas, or objects --- solves 68.0\% of the hard split, the large test
instances, against 4.0\% for \gadarbind{},
the same system with the chain layer removed and everything else identical,
and 0.0\% for the untrained control.
On the easy split, the held-out domain's own training-scale instances, it
solves 100.0\% against 16.0\% for \gadarbind{}. The step from 4\% to 68\% is
produced by a component that adds no learned parameters and no new kind of
supervision: it only states relations already present in the domain
description in a form message passing can use. This is the sense in which
the domain description is data rather than architecture --- and the sense in
which the chain layer, not the mere presence of the lifted domain in the
graph, is what makes the description actionable.

\textbf{An untrained control rules out the executor.} Coverage is measured
with a revisit monitor that resamples an applicable action when the greedy
rollout re-enters a visited state, so a natural objection is that the
monitor, not the policy, is solving the instances. We therefore evaluate the
identical system --- same graph, same decoder, same monitor, same 500-step
bound --- with a network that has seen a single epoch of training. On the
hard split it solves 0.0\%; on the easy split it reaches 26.4\%, which
calibrates exactly how forgiving small instances are under a monitored
executor and is the number the 100\% must be read against. Whatever
produces 68\% on instances where the harness alone produces nothing is in
the weights, and it was learned from seven other domains.

\textbf{The mechanism is not a terminal trigger.} An obvious deflationary
reading of a high easy-split number is that the policy has learned only to
fire when a goal atom is one action away, and that the domain's easy
instances forgive everything else. We measured that reading directly with the
achievement oracle described in the setup, which hard-codes exactly this
behavior with perfect knowledge: it bounds coverage at 50\% on easy Visitall
and at 0--5\% on Miconic. \gadar{} exceeds that ceiling by a
factor of two on Visitall. Whatever the chain layer's goal distances,
chaining edges, and grounded effect edges give the network, it is more than a
terminal trigger; the policy is choosing actions that are several steps from
achieving anything, which is what the bucketed goal distance over the enables
relation was designed to support.

\textbf{Plan quality: near-planner-length plans, not lucky wandering.} An
easy split under a monitored executor with a 500-step bound is forgiving:
aimless but legal behavior can eventually stumble into goals, so coverage
alone cannot distinguish competence from exhaustive wandering there. Plan
quality does distinguish them.
\gadar{}'s mean plan length is 30.1 steps against the satisficing planner's
29.2 on the same instances, a quality ratio of 0.97. A policy that solved
these instances by wandering under a 500-step bound would show a ratio an
order of magnitude worse. The zero-shot policy is, on this domain, producing
plans within 3\% of a planner that searches --- while itself performing no
search. On the hard split, where coverage is itself decisive, plan quality
remains 0.81.

\textbf{Transfer on a second domain.} On Miconic, \gadar{} solves 35.96\%
of the easy split at plan quality 0.63, against 2.63\% for the untrained
control under an identical executor --- a thirteen-fold gap attributable
entirely to what was learned from the other seven domains. Coverage on the
hard split, whose expert plans average 182 steps, is 0\%: at Miconic's
horizon, per-step errors that the easy split absorbs compound beyond
recovery. The transfer is real and it is partial, and we report both
facts.

\textbf{The control learns its domains and transfers nothing.} \union{},
trained on the identical data with the identical architecture and budget,
solves its training domains --- 100\% on Miconic, 85.5\% on Gripper, 68.8\%
on Grid in-domain --- and 0\% on both splits of held-out Visitall. Symbol
identity is a representation that cannot leave its vocabulary: every
capability it supports ends at the boundary of the symbols it has seen.
This is the gap the ladder attributes: moving the domain description into
the input (\gadarbind{}) makes transfer nonzero, and the chain layer
(\gadar{}) makes it large.

\textbf{A methodological note: transfer peaks early.} Zero-shot performance
is not monotone in training: the same run reaches 100.0\% coverage at epoch
190, 97.6\% at epoch 330, and degrades further by epoch 500 while training
loss falls throughout (\gadarbind{} shows the same pattern, 16.0\% at epoch
260 to 3.3\% at 500). Continued fitting of the training domains' particulars
erodes the domain-general structure, and in-domain validation cannot see it
--- it is drawn from exactly the domains being overfitted to --- so no
loss-based rule selects the epoch that transfers best. We therefore snapshot
every run at a fixed epoch cadence, evaluate every snapshot on the held-out
domain, and report the best with its epoch stated --- an existence claim,
matched to a capability question, applied identically to every rung. Any
future work in this setting will meet the same sensitivity; the reported
checkpoint's epoch is an experimental detail that must be stated.

\begin{table*}[!ht]
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.1}
\small
\centering
\caption{Zero-shot coverage (C, \%) and plan quality (P), leave-one-domain-out.
E = the held-out domain's own training-scale instances (unseen); H = its test
instances, up to an order of magnitude larger. GABAR$^\dagger$: published
per-domain GABAR trained \emph{on} the held-out domain (hard split) --- the
ceiling dedicated training sets. Ladder columns add one component each: \union{} (symbol one-hots, domain in
the weights), \gadarbind{} (+structural features, lifted domain layer,
binding edges), \gadar{} (+chain layer). \todonum{--} marks runs in flight,
not zeros.}
\label{table:main_table}
\definecolor{turqLight}{HTML}{C8F1EA}
\definecolor{turqMed}{HTML}{64D1C6}
\definecolor{turqDark}{HTML}{00A3A1}
\newcommand{\levelA}{}
\newcommand{\levelB}{\cellcolor{turqLight}}
\newcommand{\levelC}{\cellcolor{turqMed}}
\newcommand{\levelD}{\cellcolor{turqDark!80}}
\definecolor{plumLight}{HTML}{F4EBFC}
\definecolor{plumMed}{HTML}{DCC4F6}
\definecolor{plumDark}{HTML}{BBA0E5}
\newcommand{\pvalA}{\cellcolor{plumLight}}
\newcommand{\pvalB}{\cellcolor{plumMed}}
\newcommand{\pvalC}{\cellcolor{plumDark!80}}
\newcommand{\pNA}[1]{#1}
\newcommand{\pA}[1]{\pvalA #1}
\newcommand{\pB}[1]{\pvalB #1}
\newcommand{\pC}[1]{\pvalC #1}
\newcommand{\nores}{\todonum{--}}
\begin{tabular}{@{}l@{\hspace{2pt}}l|cc|cc|cc|cc@{}}
\toprule
& & \multicolumn{2}{c|}{GABAR$^\dagger$} & \multicolumn{2}{c|}{\union{}} & \multicolumn{2}{c|}{\gadarbind{}} & \multicolumn{2}{c}{\textbf{\gadar{}}} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-6} \cmidrule(lr){7-8} \cmidrule(l){9-10}
Domain & Split & C$\uparrow$ & P$\uparrow$ & C$\uparrow$ & P$\uparrow$ & C$\uparrow$ & P$\uparrow$ & C$\uparrow$ & P$\uparrow$ \\
\midrule
\multirow{2}{*}{\makebox[1.4cm][l]{Visitall}}
& E & -- & -- & \levelA 0.0 & -- & \levelA 16.0 & \nores & \levelD \textbf{100.0} & \pB{\textbf{0.97}} \\
& H & \nores & \nores & \levelA 0.0 & -- & \levelA 4.0 & \nores & \levelB \textbf{68.0} & \pB{\textbf{0.81}} \\
\midrule
\multirow{2}{*}{\makebox[1.4cm][l]{Miconic}}
& E & -- & -- & \nores & \nores & \levelA 0.0 & -- & \levelA 35.96 & \pA{0.63} \\
& H & \nores & \nores & \nores & \nores & \levelA 0.0 & -- & \levelA 0.00 & -- \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table}[!ht]
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.1}
\small
\centering
\caption{In-domain coverage (hard split): a \emph{single} policy trained on
all eight domains, per rung, against published per-domain GABAR
($\dagger$: one model per domain) --- the price of generality.
\todonum{--} marks runs in flight.}
\label{table:indomain}
\newcommand{\nres}{\todonum{--}}
\begin{tabular}{@{}l|c|ccc@{}}
\toprule
Domain & GABAR$^\dagger$ & \union{} & \gadarbind{} & \gadar{} \\
\midrule
Blocks    & \nres & \nres & 57.0 & \nres \\
Miconic   & \nres & \nres & 61.3 & \nres \\
Spanner   & \nres & \nres & 1.0 & \nres \\
Gripper   & \nres & \nres & 94.2 & \nres \\
Visitall  & \nres & \nres & 68.0 & \nres \\
Grid      & \nres & \nres & 70.8 & \nres \\
Logistics & \nres & \nres & 0.0 & \nres \\
Rovers    & \nres & \nres & 5.6 & \nres \\
\midrule
Combined  & \nres & \nres & 44.7 & \nres \\
\bottomrule
\end{tabular}
\end{table}


\section{Conclusion}

We introduced \gadar{}, a policy for classical planning that takes the domain
description as input and constructs grounded actions in domains never seen in
training, without search. The capability rests on a new representation ---
structural features, the lifted domain description as a graph, the
grounded--lifted binding layer connecting ground applicability to lifted
structure, and the chain layer that makes the domain's own producer--consumer
structure and its distance to the goal explicit --- while GABAR's encoder,
ranking decoder, and search-free execution carry over unchanged. The
representation is evaluated as it is built: a ladder of systems, each the
previous plus one commitment, so that every empirical gap certifies a
specific design decision. The binding layer is what makes ground
applicability expressible in the domain's own vocabulary --- the
information the lifted graphs of prior work lack; the chain layer is what
makes that structure reachable by message passing, and it is the gap that the
measurements isolate. On Visitall held out of training entirely, it moves
zero-shot coverage from 16\% to 100\% on training-scale instances at plan
quality 0.97, above the ceiling of a hand-coded goal-achievement oracle,
and from 4\% to 68\% on the hard split, where the untrained control solves
nothing. We evaluate Visitall and Miconic as held-out targets; the remaining
placeholder cells are runs in flight and are marked as placeholders rather
than filled in. Zero-shot
transfer is bounded by structure: renaming invariance means structurally
isomorphic domains necessarily receive identical behavior, so beyond that
ceiling adaptation needs data from the
new domain: few-shot adaptation, lifted successor generation, richer PDDL
fragments, and probabilistic settings remain future work.

\bibliography{aaai2027}

% Check whether the conference requires a reproducibility checklist to be included in the paper.
% If so, you can uncomment the following line and ajust the path to include it.
% \input{ReproducibilityChecklist.tex}

\end{document}
